{
  "name": "tech-store-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm:dev:*\" --kill-others",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspaces",
    "build:api": "npm run build --workspace=apps/api",
    "build:web": "NEXT_DISABLE_ESLINT=1 NEXT_DISABLE_TYPESCRIPT=1 npm run build --workspace=apps/web",
    "clean": "rm -rf apps/*/node_modules packages/*/node_modules node_modules",
    "lint": "npm run lint --workspaces",
    "type-check": "tsc --build"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "concurrently": "^8.2.2",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {
    "eslint": "^8.57.0",
    "lucide-react": "^0.400.0",
    "next": "14.2.10",
    "next-auth": "^4.24.7",
    "react-hot-toast": "^2.4.1",
    "recharts": "^2.12.7"
  }
}
